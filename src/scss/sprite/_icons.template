.icon {
	display: inline-block;
	width: 1em;
	height: 1em;
	transition: 0.3s ease-in-out;
}

{{#shapes}}
.icon-{{base}} {
	font-size:({{height.inner}}/20)*1rem;
	width:({{width.inner}}/{{height.inner}})*1em;
}

{{/shapes}}